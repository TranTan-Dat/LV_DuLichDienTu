@model IEnumerable<LV_DuLichDienTu.Models.Hinh_DiaDiemDuLich>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.hinh_duongdan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dddl_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.hinh_duongdan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dddl_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.hinh_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.hinh_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.hinh_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
