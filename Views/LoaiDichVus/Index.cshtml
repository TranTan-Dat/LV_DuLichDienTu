@model IEnumerable<LV_DuLichDienTu.Models.LoaiDichVu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ldv_ten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ldv_mota)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ldv_ten)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ldv_mota)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ldv_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ldv_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ldv_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
